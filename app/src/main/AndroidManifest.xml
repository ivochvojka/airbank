<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cz.airbank.airbankapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".AirbankApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Airbank.Light">

        <activity android:name=".ui.transaction.TransactionListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.transaction.TransactionDetailActivity"
                  android:theme="@style/Theme.Airbank.Light" />
    </application>
</manifest>
